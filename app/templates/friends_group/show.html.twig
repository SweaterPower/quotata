{% extends 'base.html.twig' %}

{% block title %}FriendsGroup{% endblock %}

{% block body %}
    <h1>FriendsGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friends_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ friends_group.name }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ friends_group.created ? friends_group.created|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('friends_group_index') }}">back to list</a>

    <a href="{{ path('friends_group_edit', {'id': friends_group.id}) }}">edit</a>

    {{ include('friends_group/_delete_form.html.twig') }}
{% endblock %}
